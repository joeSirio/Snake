/** @type {import('next').NextConfig} */
const nextConfig = {
    // distDir: "build",

    reactStrictMode: true,

    // Add basePath
    basePath: '/snake',
    output: "export"
}

module.exports = nextConfig
